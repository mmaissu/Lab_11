import { ProductListScreen } from '@/App';

/** Task 1: экран с FlatList, keyExtractor и компонентом ProductItem — см. `App.tsx` */
export default function Index() {
    return <ProductListScreen />;
}
